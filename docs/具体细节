很高兴看到你已经把想法落地成具体的评分系统设计了！你分配的 **30分时间 + 30分公司评价 + 40分其他** 这个框架很有道理，抓住了真实招聘信息的几个核心矛盾。我来逐条给你一些更细致的建议，希望能帮你把这个系统打磨得更准、更稳。

---

## 一、时间维度（30分）：首次发布 vs 最后更新

你提到“首次发布时间占7，最新更改时间占3”——我理解是**7:3的权重**，也就是：
- **首次发布时间（21分）**：越新鲜的岗位越可能是真招人。
- **最新更改时间（9分）**：如果刚更新过，说明HR还在维护这个岗位，但要注意“更新”可能是刷KPI。

### 我的优化建议：

1. **首次发布时间（21分）建议用分段衰减函数**，例如：
   - ≤3天 → 21分
   - 4-7天 → 15分
   - 8-14天 → 10分
   - 15-30天 → 5分
   - >30天 → 0分（直接标记为僵尸岗）

2. **最新更改时间（9分）需要警惕陷阱**：
   - 有的HR为了岗位置顶，会每隔几天**无意义地改个标点符号**来刷新发布时间。这种“虚假更新”反而说明岗位可能长期没招到人（或是假岗）。
   - **建议**：只有“更新后JD内容有明显变化”（比如岗位职责、薪资调整）才加分。否则更接近“刷KPI”，不加分甚至扣分。
   - 如果你暂时无法判断内容变化，可以保守处理：**最新更改时间得分 = 与首次发布时间相比，如果更新间隔 < 7天且不是同一天发布的，给5分；否则0分**。

3. **额外建议**：增加一个 **“招聘节奏健康度”** 的微调。
   - 如果同一公司同一职位**一周内重复发布了3次以上**（每次都是全新岗位ID），反而可能是HR在刷量，扣5分。

---

## 二、公司评价（30分）：联网搜索真实反馈

这个维度非常关键，但实现上难度最大。你需要解决两个问题：**搜什么** + **怎么打分**。

### 1. 搜索关键词建议
- 公司全名 + “坑” / “骗局” / “劳动仲裁” / “拖欠工资” / “加班严重”
- 公司全名 + “面试经验” / “面经” / “评价”
- 公司全名 + “boss直聘” + “骗子” （有些求职者会在平台曝光）

### 2. 简单的自动化打分逻辑（不需要完美，先跑起来）
你可以用Python调用requests + BeautifulSoup抓取搜索结果摘要（比如百度/谷歌的前10条），然后做**关键词匹配**：
- **负面词库**：拖欠、克扣、加班严重、PUA、画饼、血汗工厂、避雷、别去
  每匹配到1条负面词 → 扣3分（上限扣20分）
- **正面词库**：福利好、双休、不加班、氛围好、推荐
  每匹配到1条正面词 → 加2分（上限加10分）
- **官方信息**：天眼查/企查查获取“参保人数、成立年限、注册资本、有无诉讼”
  - 参保人数 < 10人 → 扣5分
  - 成立 < 1年 → 扣3分
  - 有“劳动争议”诉讼 → 扣5分

这样你不需要真的读懂长文，只要一个粗糙但有用的公司风险分。

### 3. 重要提醒：联网搜索的边界
- 很多小公司根本没有网络评价，这时候**不要给0分**，可以给一个**中性基础分（比如15分）**，然后只扣有明确负面信息的。
- 避免用过于激进的爬虫（比如频繁请求百度），容易被封IP。建议用**缓存机制**：一个公司一周只搜一次，结果存本地。

---

## 三、其他40分：JD质量 + 招聘方式 + 异常信号

这40分是你最容易通过规则或LLM实现的，也是区分“真假岗位”的核心。

### 我建议拆解为以下几个子项（示例分配）：

| 子维度 | 建议分值 | 评分规则示例 |
|--------|----------|----------------|
| **JD完整度** | 12分 | 有具体工作内容(≥3条) → 6分；有明确技能要求(≥2条) → 6分 |
| **薪资合理性** | 10分 | 范围窄且明确(如15k-18k) → 10分；宽范围(10k-20k)→5分；面议→2分；异常高薪→0分 |
| **发布主体真实** | 8分 | 显示“HR直招”或“部门负责人” → 8分；显示“猎头” → 4分；显示“中介”或空白 → 0分 |
| **无虚假/销售关键词** | 10分 | 出现“刷单/打字员/手工活/交费/培训贷/轻松月入过万”等 → 直接0分（一票否决） |

**合计：40分**

### 关于“是不是HR招”：
- 在BOSS直聘上，可以看右上角有没有“HR”或“老板”标签。猎聘上，直接标注“企业直招”或“猎头”。
- **HR直招往往比猎头岗位更急迫、更真实**（因为猎头可能会广撒网）。但猎头的高端岗位也有价值，所以不用一刀切，给个权重即可。

### 一个加分技巧：
- 如果JD里写了**准确的办公地址**（比如“上海市浦东新区XX路XX号”），说明公司至少有个真实的据点，可以加3分。很多虚假岗位会写“全国/在家办公/地点面议”。

---

## 四、最终得分与决策建议

总分 = 时间30分 + 公司30分 + 其他40分

- **≥75分**：**优先投递**。值得你花3-5分钟写定制化打招呼，甚至尝试找人内推。
- **60-74分**：**备选池**。如果当天没有高分岗位，可以手动投递，但不要投入过多精力。
- **40-59分**：**谨慎对待**。除非你非常缺面试机会，否则建议先放一放，观察公司评价变化。
- **<40分**：**直接忽略**。大概率是假岗、KPI岗、或坑人公司。

### 一个重要的“一票否决”项：
无论总分多少，只要出现以下任意一条，直接打**0分并标记为“危险岗位”**：
- 要求入职前交任何费用（培训费、押金、服装费）
- JD里出现“拉人头、发展下线、多级分销”
- 岗位名称是“文员/助理”，但工作内容是销售/地推

---

## 五、工程实现上的小建议

1. **先跑原型，再优化权重**：不要追求完美评分。先写一个最简单的脚本，抓20条真实岗位+20条你怀疑的假岗位，手动标注，然后调整权重直到你的评分能明显区分它们。
2. **把公司评价分设计成“可选模块”**：如果联网搜索不稳定或太慢，可以先不给公司维度打分（默认给15分），先把时间和JD维度做准，效果已经很不错了。
3. **记录每个岗位的打分理由**：方便你事后复盘，看看哪些规则误判了，不断迭代。

你的这个系统做出来，不仅是帮你找工作，也是一个很棒的“求职市场真实信号探测工具”。等调试稳定了，甚至可以分享给其他被虚假招聘折磨的求职者。

如果在写代码时遇到具体问题（比如爬虫被封、API调用超时、中文分词匹配），随时再来问我。祝你的脚本早日跑通，尽快逃离现在的烂公司！
非常好的决策！你选择“抓取完整语句传给AI判断”，既保留了AI理解语境的能力，又避免了单独开发复杂的技能（skill）。这个路线清晰、可行，我来帮你细化一下具体怎么做。

---

## 一、整体流程（搜索 → 提取 → AI判断）

1. **搜索公司名 + 关键词**（如“XX公司 评价”、“XX公司 坑”等，可组合多个查询词）。
2. **抓取搜索结果中前3-5条结果**，提取每条结果中的**标题 + 摘要片段**（通常搜索引擎直接返回，不需要点进页面）。这样你能获得2-3段完整的上下文，足够AI判断。
3. **组装成文本**，调用DeepSeek API，让AI输出：
   - 正面评价的加分（0 ~ +5分）
   - 负面评价的扣分（0 ~ -30分）
   - 综合风险等级和建议

---

## 二、给AI的精准Prompt（可直接复制使用）

```text
你是一个专业的招聘辅助AI。你的任务是根据以下搜索结果，判断公司是否存在值得警惕的负面信息，以及是否有明显的正面亮点。

搜索目标公司：{公司名}

搜索结果片段：
{将每个结果的标题和摘要按如下格式列出}
---
结果1：标题：xxx | 摘要：xxx
结果2：标题：xxx | 摘要：xxx
...

请严格按照以下JSON格式输出，不要有其他解释文字：

{
  "positive_score": 整数(0-5),   // 正面加分。若有明确的好评（如“福利好、双休、氛围赞”），每一条加1-2分，最高5分；无则0。
  "negative_score": 整数(-30-0), // 负面扣分。若出现“拖欠工资、诈骗、传销”等严重问题，扣20-30；出现“加班严重、PUA、试用期不交社保”等，扣10-15；出现“个别差评、一般性抱怨”，扣1-5；无明显负面则0。
  "risk_level": "high/medium/low/none",  // high=严重问题，建议直接放弃；medium=有风险需谨慎；low=轻微问题；none=无明显负面。
  "summary": "一句话总结主要发现（20字以内）"
}

注意：
- 只根据提供的搜索结果片段判断，不要编造信息。
- 如果搜索结果完全无关或无法判断， positive_score=0, negative_score=0, risk_level="none", summary="无有效信息"。
```

---

## 三、调用与打分示例

假设你搜“XX科技公司”，得到两条结果：

> 结果1：标题：XX科技加班严重吗？ | 摘要：网友吐槽：经常996，项目急的时候通宵，但加班费很少...
> 结果2：标题：XX科技面试经历 | 摘要：面试官很专业，公司环境不错，HR说双休一般不加班。

AI可能会输出：

```json
{
  "positive_score": 2,   // 环境不错、面试专业，加2分
  "negative_score": -12, // 加班严重、996，扣12分
  "risk_level": "medium",
  "summary": "加班较多，环境尚可"
}
```

然后你把这个结果整合进你的评分系统：
- 公司评价总分30分，初始给15分（中性分）。
- 最终公司分 = 15 + positive_score + negative_score（注意negative_score是负数）。
- 例如：15 + 2 - 12 = 5分（说明这家公司风险较高，需要警惕）。

如果 risk_level 是 "high"，你可以直接**一票否决**，不管其他维度分数多高。

---

## 四、工程实现上的小贴士

1. **搜索API的选择**
   - 最简单：用 `requests` + `BeautifulSoup` 模拟百度/谷歌搜索（但容易被封）。
   - 更稳定：用 `serpapi` 或 `duckduckgo_search` 库（免费额度有限，但个人使用足够）。
   - 建议先用 **DuckDuckGo HTML 搜索**（无需API key，稳定且轻量）。

   ```python
   from duckduckgo_search import DDGS
   with DDGS() as ddgs:
       results = list(ddgs.text(f"{company_name} 评价", max_results=3))
   ```

2. **提取关键片段**
   每个结果通常有 `title` 和 `body`（摘要），直接把两者拼接成一段文本即可。

3. **控制成本**
   每天调用AI的次数 = 你抓取的公司数 × 每个公司的搜索查询数。
   - 建议每个公司只搜索1次，关键词设为“公司名 评价 坑 面试”。
   - 如果搜索结果少于2条，直接给中性分，不调用AI。
   - 先用100条数据测试总成本（DeepSeek API 约 0.5元/百万token，真的很便宜）。

4. **缓存结果**
   同一公司一周内只判断一次，避免重复搜索和调用。用一个字典或简单数据库存起来。

---

## 五、关于“好评价加小分”的进一步思考

正面加分不要喧宾夺主。因为假招聘信息中，**正面评价往往可以被刷出来**（水军），而负面评价相对真实。所以：

- **加分上限设为5分**（占公司总分的1/6）。
- **只有明确、具体的正面信息才给加分**，比如“五险一金按最高比例”、“全员双休”、“不加班”。笼统的“公司不错”不加分。

这样能防止一家“刷好评”的公司拿到虚假高分。

---

## 六、最后验证

你可以先用5家你知道的公司（2家明显坑、2家正常、1家好评多）手动测试这个流程。看AI给出的 positive_score 和 negative_score 是否符合你的预期。如果不满意，微调Prompt里的扣分/加分标准。

这个方案完全可以替代单独开发skill，代码量很少（搜索+API调用不到50行），而且以后改规则只需要改Prompt，不用改代码。

如果你在写代码时遇到具体问题（比如搜索结果乱码、API返回格式不对），随时再来问我。祝你早日跑通系统，找到靠谱的下家！